using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SlowHover : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /* 
        LeanTween.moveLocal(gameObject, this.transform.position + Vector3.up * 0.15f, 1.5f)
            .setLoopPingPong()
            .setEaseInOutQuad();

        LeanTween.rotateAroundLocal(gameObject, Vector3.up, 360f, 5f)
            .setLoopClamp();
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
